# ═══════════════════════════════════════════════════
# Sentinel Shield Configuration
# ═══════════════════════════════════════════════════
# You usually don't need to edit this manually.
# Just run:  chmod +x setup.sh && ./setup.sh

# The port YOUR server is running on (the one we protect)
TARGET_PORT=3000

# The port Sentinel Shield listens on (users connect here)
SHIELD_PORT=80

# Internal URL to reach your server from Docker
BACKEND_URL=http://host.docker.internal:3000

# Redis for distributed rate-limiting
USE_REDIS=true

# Dashboard access key (CHANGE THIS!)
ADMIN_KEY=YOUR_SECURE_KEY_HERE

# Token Bucket configuration
RL_CAPACITY=20
RL_REFILL_RATE=5
RL_TTL_SECONDS=600
